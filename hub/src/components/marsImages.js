import React from "react"

const MarsRoverImages = ({url}) => {
    return (
        <div className="images">
            <img src={url} alt="" />
        </div>
    )
}

export default MarsRoverImages;